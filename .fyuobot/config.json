{
    "$schema": "./config.schema.json",
    "mcpServers": [
        {
            "name": "codegraph",
            "transport": "stdio",
            "command": "codegraph",
            "args": ["serve", "--mcp"],
            "enabled": true
        }

    ]

}
